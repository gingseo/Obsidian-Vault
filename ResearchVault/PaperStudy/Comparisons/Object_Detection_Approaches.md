---
title: "Small/Tiny Object Detection 접근법 비교"
tags: [comparison, small-object-detection]
papers: [[[2026_TIP_Unc-SOD|Unc-SOD]], [[2026_TPAMI_Detection_Oriented_Rectification|Detection_Oriented_Rectification]], [[2025_RemoteSensing_FANet|FANet]], [[2025_CVPR_Feature_Info_Driven_Gaussian|Feature_Info_Driven_Gaussian]], [[2025_ESWA_LSOD-YOLO|LSOD-YOLO]], [[2025_RSASE_RS-TOD|RS-TOD]], [[2025_arXiv_UAV-DETR|UAV-DETR]], [[2024_ECCV_SR-TOD|SR-TOD]], [[2022_CVPR_QueryDet|QueryDet]], [[2026_TGRS_FFSSTDNet|FFSSTDNet]], [[2025_RemoteSensing_CDATOD-Diff|CDATOD-Diff]], [[2020_ECCV_DETR|DETR]], [[2024_TGRS_ORFENet|ORFENet]], [[2024_TGRS_FFCA-YOLO|FFCA-YOLO]], [[2017_ICCV_Deformable_Convolutional_Networks|Deformable_Convolutional_Networks]], [[2021_ICLR_Deformable-DETR|Deformable-DETR]], [[2024_ECCV_DQ-DETR|DQ-DETR]], [[2025_JSTARS_Density-Aware-DETR|Density-Aware-DETR]], [[2026_ICASSP_IG-DETR|IG-DETR]], [[2025_arXiv_PaQ-DETR|PaQ-DETR]], [[2026_JSTARS_DQA-DETR|DQA-DETR]], [[2026_SSRN_DQP-DETR|DQP-DETR]], [[2026_Neural-Networks_YOFOR|YOFOR]], [[2025_TGRS_BAFNet|BAFNet]], [[2026_JSTARS_RTP-Net|RTP-Net]], [[2026_arXiv_CoLR-Det|CoLR-Det]], [[2025_TMM_Cross-DINO|Cross-DINO]]]
created: 2026-08-04
updated: 2026-09-15
---

# 비교 축
작은 객체 탐지 성능을 어디에 개입해서 끌어올리는지 — feature 표현 강화, label assignment/sampling, 연산 가속(sparse computation), 아키텍처 경량화, end-to-end 구조 개선 중 어느 축에 해당하는지로 분류한다.

# 비교표

| 논문                                   | 개입 지점                             | 핵심 방법                                                                                                           | Baseline                    | 대표 벤치마크/지표                                                   | 비고                                                             |
| ------------------------------------ | --------------------------------- | --------------------------------------------------------------------------------------------------------------- | --------------------------- | ------------------------------------------------------------ | -------------------------------------------------------------- |
| [[2026_TIP_Unc-SOD|Unc-SOD]]                          | Label assignment (RPN sampling)   | 예측 박스를 Gaussian으로 모델링해 instance-level uncertainty를 IoU 대신 동적 positive 기준으로 사용 + 두 pyramid level feature 융합(P&I) | Faster R-CNN                | SODA-D AP 28.9→31.0, SODA-A AP 32.5→34.8                     | 5개 벤치마크에서 일관된 개선, 다른 detector(Cascade R-CNN 등)로도 이식 검증         |
| [[2026_TPAMI_Detection_Oriented_Rectification|Detection_Oriented_Rectification]] | Feature 복원                        | 작은 객체의 열화 패턴을 명시적으로 학습(degradation basis)한 뒤, MoE 스타일 동적 prompt로 탐지 지향적 feature rectification 수행                | 다수 detector(plug-in)        | SODA-D AP 28.9→30.8 (Faster R-CNN), COCO AP 42.3→44.3 (TOOD) | pixel fidelity가 아니라 "탐지에 유리한 방향"으로 복원한다는 점이 SR-TOD류와 다름        |
| [[2025_RemoteSensing_FANet|FANet]]                            | Feature 강화 (주파수 영역)               | FPN과 RoI head에 DFT/DCT 기반 attention 모듈(MSFFEM, CAREM) 삽입 + 다방향 flip 증강(SAS)                                     | Faster R-CNN, RFLA 등        | AI-TOD AP 20.6→24.8                                          | 원격탐사(항공뷰) 특화, 공간 domain이 아니라 주파수 domain에서 정보를 취함               |
| [[2025_CVPR_Feature_Info_Driven_Gaussian|Feature_Info_Driven_Gaussian]]     | Feature 강화 (정보이론+위치)              | information map(정보량 기반)과 position Gaussian Mixture map 두 가지로 feature를 동시 강화                                     | 다수 detector(plug-in)        | VisDrone/AI-TOD/AI-TODv2에서 일관된 AP 개선                         | "위치를 어디에 둘지"와 "얼마나 강조할지"를 분리해서 모델링                             |
| [[2024_ECCV_SR-TOD|SR-TOD]]                           | Feature 강화 (self-reconstruction)  | neck에 self-reconstruction head를 붙여 원본-재구성 difference map을 생성, 정보 손실이 큰(=작은 물체가 있을 확률이 높은) 영역을 강조                | 다수 detector(plug-in)        | DroneSwarms/VisDrone2019/AI-TOD에서 개선                         | 생성 모델 없이 reconstruction 자체의 부산물(difference)만 활용 — 가장 가벼운 방식    |
| [[2025_RSASE_RS-TOD|RS-TOD]]                           | Feature 강화 (attention) + 헤드 추가    | RSAM(채널+공간 attention)을 각 detection head 앞에 삽입, 160×160 고해상도 tiny-object 전용 head 추가                              | YOLOv8n                     | SODA-A mAP50 60.10, AI-TOD mAP50 59.84                       | 원격탐사 특화. 헤드를 아예 새로 추가한다는 점에서 FANet/SR-TOD와 다름                  |
| [[2025_ESWA_LSOD-YOLO|LSOD-YOLO]]                        | 아키텍처 경량화                          | P5 검출층 제거 + cross-layer connection(LCOR)으로 고해상도 P2 정보 보존, 기존 경량화 기법(LSKA/NAM/DySample) 재조합                      | YOLOv8s                     | VisDrone mAP0.5 +2.5%p, 파라미터 −65.5%                          | 성능 개선보다 "성능 유지하며 경량화"가 핵심 목표. 유일하게 FLOPs/FPS를 정면으로 다룸          |
| [[2025_arXiv_UAV-DETR|UAV-DETR]]                         | End-to-end 구조 개선                  | 공간+주파수 도메인을 함께 쓰는 3개 모듈(MSFF-FE, FD, SAC)을 RT-DETR에 추가                                                          | RT-DETR                     | VisDrone AP 26.7→29.8                                        | 유일한 DETR 계열(anchor-free, NMS-free). 실시간성과 성능의 트레이드오프를 명시적으로 보고 |
| [[2022_CVPR_QueryDet|QueryDet]]                         | 연산 가속 (sparse computation)        | 저해상도 feature 예측으로 고해상도 sparse convolution 위치를 좁히는 Cascade Sparse Query                                          | RetinaNet/FCOS/Faster R-CNN | COCO FPS 4.85→14.88(정확도 손실 거의 없음), VisDrone FPS 1.16→2.75    | 유일하게 "정확도 개선"이 아니라 "동일 정확도를 더 빠르게"가 목표. Feature 강화 계열과 직교적     |
| [[2026_TGRS_FFSSTDNet|FFSSTDNet]]                       | Feature 강화(SR) + 연산 가속(patch 필터링) | CFD 모듈로 배경 patch를 걸러 연산량 절감 + FSR 모듈(학습시에만 존재하는 auxiliary reconstruction branch)로 backbone 고해상도 학습 유도           | 다수 detector(plug-in)        | FAIR1M mAP 43.88→46.25, Fps +20~30%                          | Full-scene 위성 이미지 특화. Feature 강화와 연산 가속 두 축을 동시에 다루는 유일한 논문    |
| [[2025_RemoteSensing_CDATOD-Diff|CDATOD-Diff]]                      | Label assignment + 회귀 손실          | RFLA의 Gaussian receptive field 매칭을 CLIP 의미 정보로 조건화한 diffusion denoising으로 확장 + 스케일 적응 corner-IoU 손실(BC-IoU)     | FCOS                        | AI-TOD AP 16.3(RFLA)→19.4, MSAR-1.0 AP 63.4→64.1             | SAR 영상 특화. 이 위키에서 VLM(CLIP)을 label assignment에 결합한 첫 사례        |
| [[2024_TGRS_ORFENet|ORFENet]]                          | Feature 강화(자기지도 재구성) + 다중 receptive field | 학습시에만 존재하는 이진 마스크 재구성 branch(ORB)로 정보 손실 억제 + 3개 receptive field(3×3/7×7 DW/21×21 DW)를 동적 가중합하는 MRFAFEM | FCOS(P2)                    | AI-TODv2 AP 17.3→24.8(36 epoch), LEVIR-Ship AP50 83.3(SOTA) | ORB는 FFSSTD-Net의 FSR처럼 추론 시 완전히 제거되나, 재구성 target이 이진 마스크(SR-TOD의 difference map보다 단순) |
| [[2024_TGRS_FFCA-YOLO|FFCA-YOLO]]                        | Feature 강화(다중 attention) + 아키텍처 경량화 | FEM(atrous multibranch 지역 문맥 확장) + FFM(BiFPN에 채널별 학습 가중 융합 CRC 적용) + SCAM(GCNet/SCP 계열 전역 채널·공간 attention), PConv 기반 경량판(L-FFCA-YOLO) | YOLOv5m                     | VEDAI mAP50 0.723→0.748, AI-TOD mAP50 0.537(HANet)→0.617, USOD mAP50 0.873→0.909 | LSOD-YOLO와 함께 경량화를 다루는 두 번째 논문이지만, 방식이 다르다(헤드 재배치 vs PConv backbone 재구성). 세 모듈 모두 plug-and-play로 다른 detector에도 이식 가능 |
| [[2024_ECCV_DQ-DETR|DQ-DETR]]                          | Dynamic query (density map 기반, 분류)  | Density map을 4단계로 분류해 query 개수(300/500/900/1500) 선택 + 동일 map으로 encoder feature spatial/channel attention 강화 + top-K 선별로 query content·position 생성 | Deformable DETR              | AI-TOD-V2 AP 25.9(DINO)→30.2                                  | Dynamic query DETR 계열의 원조. 회귀 대신 분류를 택해 2단계 학습 필요            |
| [[2025_JSTARS_Density-Aware-DETR|Density-Aware-DETR]]               | Dynamic query (density map 기반, 회귀) | Crowd counting 기법 응용한 연속 density map 회귀(Density Focal Loss) + balance factor로 query 수 결정 + static+동적 mix selection + anchor L1(log-ratio) 손실 | DINO/Deformable DETR         | AI-TOD-v2 mAP 30.2(DQ-DETR)→32.1                              | DQ-DETR과 직접 대조 실험(같은 baseline)으로 회귀의 우위 실증. From scratch 학습 가능 |
| [[2026_ICASSP_IG-DETR|IG-DETR]]                          | Dynamic query (density map 기반, 세분화 분류) | DQ-DETR의 4단계를 6단계로 세분화(HIP) + 곱셈 대신 덧셈 residual(1+W) feature 강화(IFE) + salient seed top-K 선택(IGQ) | DINO                         | AI-TOD-V2 AP 25.9(DINO)→30.9                                  | DQ-DETR과 가장 유사한 구조에 미세한 차이(결합 방식)로 차별화. ICASSP 짧은 논문     |
| [[2025_arXiv_PaQ-DETR|PaQ-DETR]]                         | Dynamic query (query 표현 재구성 + 품질 기반 supervision 확장)   | 소수(m=50~150)의 학습되는 공유 base pattern을 이미지 조건부로 볼록결합해 query 내용을 구성(표현 측 activation imbalance 완화) + 중간 decoder layer에서 예측 품질(IoU−분류 신뢰도 일치도)에 따라 GT당 positive 수를 동적으로 정하는 one-to-many assignment(supervision 측 불균형 완화), 최종 layer는 표준 1:1 유지 | DINO++                         | COCO val2017 12-epoch mAP 50.3→51.9, query activation Gini 계수 0.97→0.89    | 유일하게 COCO 일반 탐지에서 검증. query "개수"가 아니라 query "표현 구성 방식"과 "supervision 분배 방식"을 이미지·품질 조건부로 동적화 — 후보 병합·제거(clustering/pruning)는 하지 않는다 |
| [[2026_JSTARS_DQA-DETR|DQA-DETR]]                         | Dynamic query (병합, oriented detection) | 4단계 밀도 분류(ACP, 병합 개수의 대략적 사전) + class-agnostic rotated-NMS로 대표 선별(ACS) + attention으로 나머지 query 정보 흡수(QA, 제거 아닌 병합) | Rotated-DINO                 | DOTA-v1.0 mAP 75.59→78.29, DOTA-v1.5 mAP 68.79→71.53          | 유일한 oriented(회전) object detection. One-to-one matching의 고품질 negative 문제를 이론적으로 분석(focal loss gradient 유도) |
| [[2026_SSRN_DQP-DETR|DQP-DETR]]                         | Dynamic query (density map, encoder 주입+토큰 순위) | ADPG(multi-branch density map 생성) + BCME(양방향 cross-modulation으로 encoder memory 직접 강화) + RCS(density+classification 결합 토큰 우선순위, margin ranking loss로 직접 감독) | D-Fine-S                     | VisDrone AP50 42.4→46.4, AI-TOD AP50 35.6→38.7                | Density 정보를 query 개수뿐 아니라 encoder 표현·토큰 순위까지 확장한 가장 포괄적 사례. SSRN 프리프린트 |
| [[2026_Neural-Networks_YOFOR|YOFOR]]                            | Coarse-to-fine 서브영역 + 배경 억제 + long-tailed 완화 | ALSM(coarse box 클러스터링 기반 적응적 서브영역 크롭) + FEM(recursive Gaussian filter로 배경 흐림) + CBM(공간 semantic 보존 tail class 복제) | YOLOv8s/YOLOv5s/Faster-RCNN(plug-in) | VisDrone AP 19.1→52.8, DOTA AP 29.2→53.7, AI-TOD AP 21.8→70.2 | 세 문제(서브영역 품질, 배경 간섭, long-tailed)를 동시에 다루는 유일한 논문. 전 모듈 비지도·plug-and-play |
| [[2025_TGRS_BAFNet|BAFNet]]                           | Feature 강화(전경+배경 이중 attention) + boundary auxiliary supervision | DSAM(고레벨 feature로 전경 attention + 그 여집합인 배경 attention을 동시에 생성해 저레벨 feature 강화) + Boundary-Aware Branch(Laplacian pyramid GT로 경계 예측을 auxiliary supervision) | DetectoRS(RFP+RFLA) | AI-TOD AP_vt 12.8→16.6, VisDrone AP 26.6~29.2→30.8, DIOR mAP 76.2→78.1, LEVIR-Ship AP50 83.3→84.2 | 유일하게 배경을 별도 attention으로 명시 모델링. 네 벤치마크 동시 검증한 유일한 논문 |
| [[2026_JSTARS_RTP-Net|RTP-Net]]                          | Feature 강화(수용영역-texture 협업 최적화) + 경량화 | GLEM(대·소 커널 병렬 depthwise conv로 소스 단계부터 전역 문맥+국소 texture 동시 추출) + AWEM(다중 pooling 기반 texture 적응적 복원) + MSAF(CBAM 기반 스케일 간 융합 정제) | YOLOv8 | DIOR mAP 83.3(YOLOv8)→84.1, AI-TOD mAP50:95 0.148(최고 경쟁)→0.166, GFLOPs −27.2% | 정확도 개선과 경량화(GFLOPs·파라미터·FPS 전부)를 동시에 달성한 유일한 사례 |
| [[2026_arXiv_CoLR-Det|CoLR-Det]]                         | Latent 정규화(학습 전용 SR) + saliency 기반 non-destructive token routing | 학습 전용 latent restoration branch로 encoder를 SR loss로 정규화(추론 시 완전 제거) + saliency-guided token routing(저saliency 토큰은 제거 아닌 bypass로 attention만 생략) + detection-prioritized 2단계 학습 | Swin-T DINO | NWPU VHR-10 AP 0.766→0.836(AP_s +16.2%p), DOTAv1.5 AP_s +5.8%p, HRSSD AP_s +5.6%p | 저해상도(2× downsampling) 원격탐사 특화. Detached 실험으로 "encoder 공동 업데이트"가 핵심 메커니즘임을 직접 증명 |
| [[2025_TMM_Cross-DINO|Cross-DINO]]                        | Feature 강화(backbone-encoder 재결합) + label/loss 재설계 | Short+long range를 함께 집계하는 deep MLP 백본(CLAP-Strip-MLP) + backbone·encoder feature를 두 단계 gating으로 교차 인코딩하는 CCTM으로 encoder의 feature blurring 완화 + 객체 크기·클래스를 결합한 Category-Size 소프트 라벨 기반 Boost Loss로 소형 객체 클래스 예측 점수 강화 | DINO | COCO AP_S 32.0→36.4(12epoch, CLAP-Strip-T), SODA-D AP 27.7→32.0, VisDrone AP 31.7→35.4 | End-to-end 구조 계열([[2025_arXiv_UAV-DETR|UAV-DETR]])에도 동시에 속하는 두 번째 DETR/DINO 계열. Feature 강화와 loss 재설계를 한 논문에서 동시에 다루는 첫 사례. AI-TOD처럼 극소형 객체가 지배적인 데이터셋에서는 오히려 baseline보다 낮은 AP를 보이는 예외 존재 |

# 분석
- **DETR·Deformable Convolutional Networks·Deformable DETR은 비교표에서 제외**: [[2020_ECCV_DETR|DETR]], [[2017_ICCV_Deformable_Convolutional_Networks|Deformable_Convolutional_Networks]], [[2021_ICLR_Deformable-DETR|Deformable-DETR]]는 이 비교축(기존 detector에 어떻게 개입하는지)의 대상이 아니라, 이후 여러 논문이 기반하는 계보의 출발점(순수 foundational 아키텍처/모듈)이라 위 표에는 넣지 않았다. 나머지 논문들이 "무엇에 개입하는가"를 다루는 반면, 이 셋은 애초에 "개입할 기존 구조 자체(anchor·NMS 파이프라인, CNN의 고정 grid 샘플링, DETR의 전역 attention)"를 재정의한 논문이라는 점에서 층위가 다르다. Deformable DETR은 DETR의 느린 수렴·소형 객체 열세 문제를 deformable attention(Deformable Convolutional Networks의 offset 아이디어를 attention으로 이식)으로 해결한 논문으로, 뒤이어 처리하는 DQ-DETR 등 dynamic query DETR 계열 6편의 공통 baseline이다.
- **공통 경향**: 13편 중 10편이 "기존 detector에 plug-in 모듈을 추가"하는 방식(FANet, feature-info-driven-gaussian, sr-tod, rs-tod, detection-oriented-rectification, unc-sod, ffsstd-net, cdatod-diff, orfenet, ffca-yolo)이며, 새 아키텍처를 처음부터 설계한 논문은 없다 — 이 분야는 지금 대부분 "기존 백본/헤드를 어떻게 보강할 것인가"에 집중되어 있다.
- **Feature 강화 계열 내부의 차이**: FANet(주파수), feature-info-driven-gaussian(정보량+위치), sr-tod(reconstruction 부산물), rs-tod(공간 attention), ffsstd-net(SR 기반 auxiliary branch), orfenet(이진 마스크 재구성 + 다중 receptive field), ffca-yolo(atrous 지역 문맥 + 채널별 학습 가중 융합 + GCNet 계열 전역 attention), bafnet(전경+배경 이중 attention + boundary auxiliary supervision), rtp-net(수용영역-texture 협업 최적화), colr-det(latent SR 정규화 + saliency 기반 non-destructive routing)는 모두 "약한 feature를 어떻게 보강할지"를 다루지만 신호를 얻는 소스가 다르다 — 이 아홉 편을 하나로 묶어 "어떤 신호가 실제로 tiny object의 위치/존재를 가장 잘 드러내는가"라는 질문으로 다시 비교해볼 가치가 있다. 특히 orfenet은 ablation에서 "다중 소스를 쓴다"는 것 자체의 기여가 "동적으로 가중한다"는 정교화보다 훨씬 크다는 점을 수치로 보여줘(Table III, +0.9 vs +0.2), 이 계열 전체에 반복되는 패턴을 뒷받침한다. bafnet은 이 패턴에서 예외적인데, DSAM(+2.3)과 Boundary-Aware Branch(+2.7)의 개별 기여가 비슷한 수준으로 나뉜다 — 두 모듈이 서로 이질적인 정보(전경/배경 문맥 vs 경계 디테일)를 다루기 때문일 가능성이 있다. colr-det도 유사한 패턴을 보이는데, saliency 라우팅 단독으로는 AP_s가 오히려 하락하고(0.487→0.474) SR 브랜치와 결합해야만 크게 개선(0.649)되어(Table VI), "이질적 신호 간 시너지"가 "단일 신호의 정교화"보다 중요하다는 점이 이 계열에서 반복 확인된다.
- **연산 가속(sparse computation) 축이 새로 생김**: querydet과 ffsstd-net(CFD 모듈)은 "무엇을 계산할지"를 좁혀 연산량 자체를 줄인다는 공통점이 있다 — querydet은 FPN 레벨 간 sparse convolution, ffsstd-net은 patch 단위 필터링으로 구현 층위는 다르지만 동일한 coarse-to-fine 사상을 공유한다. 나머지 논문들이 대부분 "정확도"만 보고하는 데 비해 이 두 편은 FPS/연산 비용을 정면으로 다룬다는 점에서 lsod-yolo(경량화 축)와 목적의식이 겹친다.
- **label assignment 축에 cdatod-diff 추가**: unc-sod에 이어 cdatod-diff도 "어떤 prior를 positive로 볼지"를 동적으로 바꾸는 계열에 속한다. 다만 unc-sod는 예측 박스의 uncertainty를, cdatod-diff는 CLIP의 의미 정보를 기준으로 삼는다는 점에서 신호의 출처가 다르다 — 두 방법을 결합하면 "기하학적으로도, 의미적으로도 타당한" 샘플링이 가능할 수 있다.
- **원격탐사(항공뷰)·SAR 특화 vs 범용**: FANet, rs-tod, uav-detr, ffsstd-net은 원격탐사/드론뷰 이미지에, cdatod-diff는 SAR 영상에 특화되어 있고, 나머지(unc-sod, detection-oriented-rectification, feature-info-driven-gaussian, sr-tod, lsod-yolo, querydet)는 SODA-D 같은 지상 시나리오까지 포괄하는 범용 벤치마크를 함께 본다. 항공뷰 특화 논문들은 공통적으로 "주파수 도메인" 정보를 쓰는 경향이 있다(FANet, uav-detr) — 항공 이미지의 반복적/주기적 텍스처 특성과 관련 있을 가능성. SAR는 이번에 cdatod-diff로 처음 다뤄진 센서 도메인.
- **경량화·연산 효율 축 확장**: lsod-yolo(파라미터 경량화)에 이어 querydet·ffsstd-net(연산 가속), ffca-yolo(PConv 기반 backbone 재구성), rtp-net(depthwise separable 병렬 커널)까지 더해지며 "정확도"만이 아니라 "효율"을 정면으로 다루는 논문이 다섯 편으로 늘었다 — 실제 배포(엣지/실시간) 관점에서 이 다섯 편을 하나의 갈래로 놓고 비교할 가치가 있다. 특히 lsod-yolo와 ffca-yolo는 둘 다 "성능 유지하며 경량화"를 목표로 하지만 전략이 상반된다 — lsod-yolo는 저기여 검출 헤드(P5) 자체를 제거하는 구조적 재배치인 반면, ffca-yolo(L-FFCA-YOLO)는 backbone 컨볼루션 연산을 PConv로 대체하는 연산 층위의 경량화다. rtp-net은 이 중 유일하게 "정확도 개선"과 "GFLOPs/파라미터/FPS 동시 개선"을 모두 정량적으로 입증한 사례로, 별도의 경량판(L-FFCA-YOLO, LSOD-YOLO처럼)을 만들지 않고도 기본 설계 자체로 이를 달성했다는 점이 특이하다.
- **DETR → RT-DETR/DINO → UAV-DETR/Cross-DINO 계보**: DETR이 2020년에 처음 지적한 "소형 객체(APS) 성능 열세"라는 한계는 이 위키의 거의 모든 feature 강화 논문이 (다른 아키텍처에서) 다루는 문제와 동일한 문제의식이다. UAV-DETR은 RT-DETR(DETR의 실시간화 후속작)을, Cross-DINO는 DINO(DETR의 노이즈 디노이징 후속작)를 기반으로 각각 주파수 도메인 모듈과 backbone-encoder 재결합+크기 인지 loss를 추가해 이 한계에 대응한다 — 두 논문 모두 원조 DETR이 아니라 그 개선 후속작을 baseline으로 삼는다는 공통점이 있고, 원조 DETR 자체에 이 위키의 feature 강화 기법을 직접 적용한 사례는 아직 없다.

## Dynamic Query DETR 계열 (DQ-DETR, Density-Aware DETR, IG-DETR, PaQ-DETR, DQA-DETR, DQP-DETR)

Deformable DETR의 극도로 느린 수렴과 소형 객체 열세를 해결하는 과정에서 파생된 6편은 모두 "DETR의 object query를 이미지 내용에 따라 동적으로 조정한다"는 상위 아이디어를 공유하지만, 실제로 **어떤 신호를 쓰고 그 신호를 어디에 적용하는지**가 뚜렷이 갈린다. 이를 기준으로 두 하위 갈래로 나뉜다.

**하위 갈래 A — 전역 밀도(density map) 기반, [[Density_Guided_Dynamic_Query]] 개념** (DQ-DETR, Density-Aware DETR, IG-DETR, DQP-DETR 4편)
이미지 전체의 인스턴스 분포를 요약한 density map으로 "몇 개의 query를 어디에 배정할지"를 정한다. 4편 사이에서도 밀도 신호를 다루는 방식이 점차 확장되어 왔다:
- **query 개수 산정 방식**: DQ-DETR은 4단계 이산 분류(회귀 시 AP 25.9→14.9로 급락하는 것을 직접 확인해 분류를 채택), IG-DETR은 이를 6단계로 세분화, Density-Aware DETR은 반대로 crowd counting 기법을 응용한 연속 회귀(Density Focal Loss)로 전환해 DQ-DETR과 동일 baseline(DINO) 위에서 직접 대조 실험을 수행하고 회귀의 우위(30.2→32.1 AP)를 실증했다. DQP-DETR은 다시 회귀 기반 density map을 쓰되 query 개수뿐 아니라 그 자체를 encoder memory와 토큰 순위에까지 확장한다.
- **density 신호의 적용 범위**: DQ-DETR·IG-DETR은 density map을 query 개수 산정과 encoder feature 강화(전자는 곱셈 마스킹, 후자는 덧셈 residual `1+W`) 두 곳에 쓴다. DQP-DETR은 여기에 (1) encoder memory로의 양방향 cross-modulation 주입(BCME), (2) 토큰별 우선순위를 GT 밀도 기반 참조와 비교해 직접 감독하는 margin ranking loss(RCS)까지 더해 이 계열에서 가장 포괄적으로 밀도 정보를 활용한다.
- **공통 관찰**: 4편 모두의 ablation에서 "다중 신호를 쓴다는 것 자체"(feature 강화·density 주입)의 기여가 "그 신호를 얼마나 정교하게 다루는가"(세분화, 회귀 vs 분류 등)의 기여보다 일관되게 크다 — 예컨대 DQP-DETR은 ADPG+BCME만으로 AP +0.6인데 RCS(토큰 순위 학습) 추가로 +2.1이 더 붙고, Density-Aware DETR은 IDE&QA(밀도 기반 개수 조정)만으로 전체 개선폭의 대부분(+4.6/+6.1)을 차지한다.

**하위 갈래 B — query 개수가 아니라 query 자체를 재구성** (PaQ-DETR, DQA-DETR 2편)
전역 밀도로 "몇 개"를 정하는 갈래 A와 달리, 이미 있는 query의 표현·정보를 어떻게 다룰지에 집중한다. 다만 둘의 구체적 메커니즘은 서로 다른 축이다.
- PaQ-DETR: query 개수는 고정(n=300/900)한 채, [[Pattern_Quality_Aware_Query_Refinement]] 개념대로 (1) query *표현*을 소수의 공유 base pattern의 이미지 조건부 볼록결합으로 구성해 표현 측 activation imbalance를 줄이고, (2) 예측 품질 기반 1:多 assignment로 supervision 측 불균형을 줄인다 — 후보를 병합하거나 제거하는 과정 자체가 없다. 유일하게 COCO 일반 객체 탐지에서 검증(다른 5편은 항공/원격탐사 특화 벤치마크 중심)했다는 점도 독보적.
- DQA-DETR: 유일한 oriented(회전) object detection 사례. One-to-one matching에서 유사 고품질 query가 서로 negative로 경쟁하며 gradient를 왜곡한다는 것을 focal loss 수식으로 직접 증명하고(6편 중 유일한 이론적 분석), class-agnostic rotated-NMS로 대표 query를 선별한 뒤 선택되지 않은 query도 attention으로 흡수·**병합**(Query Aggregator, 정보 손실 없음)한다는 점에서 PaQ-DETR과는 전혀 다른 문제(query 개수를 실질적으로 압축하는 것)를 푼다 — 병합 대상 선정에 쓰는 4단계 밀도 분류(ACP)는 하위 갈래 A의 요소도 일부 포함하는 경계 사례.

**갈래 간 비교**: 하위 갈래 A는 "얼마나 많은 query가 필요한가"라는 양(quantity)의 문제에 집중한다. 하위 갈래 B의 두 논문은 서로 다른 층위의 문제를 푸는데, DQA-DETR은 "이미 있는 query 중 무엇을 병합해 개수를 실질적으로 압축할 것인가"라는 구성(composition)의 문제를, PaQ-DETR은 "query의 표현 방식과 학습 신호 분배 방식을 어떻게 재설계할 것인가"라는 query 자체의 설계 문제를 다룬다 — 둘 다 "개수"를 직접 조정하지 않는다는 점에서 갈래 A와 대비되지만, 서로도 직접 비교 대상은 아니다. DQA-DETR과 [[2022_CVPR_QueryDet|QueryDet]]의 Table 8(query 900→2400 확장 시 baseline 성능 붕괴, 55.29까지 하락)은 이 위키 전체를 통틀어 "query를 단순히 늘리는 것은 위험하고 그것을 어떻게 다루는가가 핵심"이라는 메시지를 가장 극적으로 보여주는 실험이기도 하다. 세 관점(개수/구성/표현·supervision)은 상호 배타적이지 않아, "전역 밀도로 대략의 예산을 정하고, 유사 query는 병합하고, 남은 query의 표현·supervision은 패턴·품질 기반으로 재설계"하는 3단 하이브리드가 자연스러운 다음 단계로 보인다.

- **YOFOR — coarse-to-fine 서브영역 국소화라는 별도 축**: [[2026_Neural-Networks_YOFOR|YOFOR]]은 위 dynamic query DETR 계열과 "밀집 영역에 자원을 더 쓴다"는 상위 목표는 공유하지만, DETR query가 아니라 이미지/픽셀 공간에서 직접 클러스터링으로 서브영역을 크롭한다는 점에서 완전히 다른 층위의 해법이다. [[2022_CVPR_QueryDet|QueryDet]](feature pyramid 레벨 sparse query)·[[2026_TGRS_FFSSTDNet|FFSSTDNet]](patch 단위 CFD 필터링)과 함께 "어디를 볼지 좁힌다"는 세 번째 그룹을 형성하며, 여기에 배경 억제(FEM)와 long-tailed 클래스 완화(CBM)까지 한 파이프라인에 묶은 것이 특징이다. Long-tailed 문제를 다룬 것은 이 위키에서 YOFOR가 유일하다.
